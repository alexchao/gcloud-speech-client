# gcloud-speech-client
Basic Python client for Google Gloud Speech API

Currently, requires the audio to be on Google Cloud Storage.

Usage:
```
python transcribe.py gs://bucket-name/foo.flac output.json
```
